# ═══════════════════════════════════════════════════════════════════
# AEON NEXUS .gitignore
# ═══════════════════════════════════════════════════════════════════
# This file tells Git which files to NEVER upload to GitHub
# Protects: Secrets, temporary files, build artifacts, local config
# ═══════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────
# 🐍 PYTHON FILES
# ───────────────────────────────────────────────────────────────────
# Auto-generated files from running Python code

__pycache__/              # Python bytecode cache folders
*.py[cod]                 # Compiled Python files (.pyc, .pyo, .pyd)
*$py.class                # Python class files
*.so                      # Shared object files (compiled libraries)
.Python                   # Python installation folder
build/                    # Build output folder
develop-eggs/             # Development eggs folder
dist/                     # Distribution folder
downloads/                # Downloads folder
eggs/                     # Egg files folder
.eggs/                    # Eggs cache folder
lib/                      # Library folder
lib64/                    # 64-bit library folder
parts/                    # Buildout parts
sdist/                    # Source distribution
var/                      # Variable data
wheels/                   # Wheel files
*.egg-info/               # Package metadata
.installed.cfg            # Installation config
*.egg                     # Python egg files

# ───────────────────────────────────────────────────────────────────
# 🌐 VIRTUAL ENVIRONMENTS
# ───────────────────────────────────────────────────────────────────
# Each developer creates their own - don't share these!

venv/                     # Standard virtual environment name
ENV/                      # Alternative venv name
env/                      # Another common venv name
.venv                     # Hidden venv folder

# ───────────────────────────────────────────────────────────────────
# 💻 IDE & EDITOR FILES
# ───────────────────────────────────────────────────────────────────
# Personal editor settings - each developer uses their own

.vscode/                  # VS Code settings
.idea/                    # PyCharm/IntelliJ settings
*.swp                     # Vim swap files
*.swo                     # Vim swap files (alternative)
*~                        # Backup files created by editors

# ───────────────────────────────────────────────────────────────────
# 🖥️ OPERATING SYSTEM FILES
# ───────────────────────────────────────────────────────────────────
# OS-specific files that shouldn't be shared

.DS_Store                 # macOS folder metadata
Thumbs.db                 # Windows thumbnail cache

# ───────────────────────────────────────────────────────────────────
# 📝 LOG FILES
# ───────────────────────────────────────────────────────────────────
# Runtime logs - can contain sensitive data and grow large

*.log                     # All log files
logs/                     # Logs directory

# ───────────────────────────────────────────────────────────────────
# 🔐 ENVIRONMENT VARIABLES & SECRETS
# ───────────────────────────────────────────────────────────────────
# ⚠️ CRITICAL: These files contain passwords, keys, tokens!

.env                      # Environment variables (passwords, keys)
.env.local                # Local environment overrides
*.key                     # Any key files

# ───────────────────────────────────────────────────────────────────
# 🗄️ DATABASE FILES
# ───────────────────────────────────────────────────────────────────
# Database files contain actual data - never upload!

*.db                      # SQLite database files
*.sqlite                  # SQLite database files (alt extension)
*.sqlite3                 # SQLite database files (alt extension)

# ───────────────────────────────────────────────────────────────────
# 📊 RATE LIMITING STORAGE
# ───────────────────────────────────────────────────────────────────
# Runtime data for rate limiting - local only

data/rate_limits/         # Rate limit tracking files

# ───────────────────────────────────────────────────────────────────
# 🔒 SECRETS & CONFIGURATION
# ───────────────────────────────────────────────────────────────────
# ⚠️ EXTREMELY IMPORTANT: Never upload these files!

config.json               # Your actual configuration with keys
master_keys.json          # Master keys for authentication
secrets.json              # Any secrets file

# ───────────────────────────────────────────────────────────────────
# 📦 NODE.JS FILES (if using JavaScript tools)
# ───────────────────────────────────────────────────────────────────

node_modules/             # NPM packages (huge folder!)
package-lock.json         # NPM lock file

# ───────────────────────────────────────────────────────────────────
# 🗑️ TEMPORARY FILES
# ───────────────────────────────────────────────────────────────────

tmp/                      # Temporary directory
temp/                     # Temporary directory (alternative)
*.tmp                     # Temporary files

# ───────────────────────────────────────────────────────────────────
# 🧪 TESTING & COVERAGE
# ───────────────────────────────────────────────────────────────────

htmlcov/                  # HTML coverage reports
.coverage                 # Coverage data file
.coverage.*               # Coverage data files (alternative)
coverage.xml              # Coverage XML report
*.cover                   # Coverage files

.pytest_cache/            # Pytest cache folder
.tox/                     # Tox testing environments

# ───────────────────────────────────────────────────────────────────
# 📚 DOCUMENTATION BUILDS
# ───────────────────────────────────────────────────────────────────

docs/_build/              # Sphinx documentation build
site/                     # MkDocs build output

# ═══════════════════════════════════════════════════════════════════
# END OF .gitignore
# ═══════════════════════════════════════════════════════════════════
